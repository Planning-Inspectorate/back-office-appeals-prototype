<div class="app-sort">
  <p>
    <b class="govuk-!-margin-right-1">Sort by:</b>
    {% if data.sort == 'Due date' %}
      <span class="">Due date</span>
    {% else %}
      <a  href="?sort=Due date">Due date</a>
    {% endif %}
    <span class="govuk-!-margin-left-1 govuk-!-margin-right-1">|</span>
    {% if data.sort == 'Status' %}
      <span>Status</span>
    {% else %}
      <a href="?sort=Status">Status</a>
    {% endif %}
  </p>
</div>